HOWTO
=====

0. Copy models folder to some location on harddisk

1. Edit sys_config.yaml and ajust settings to reflect system configuration

OBJECT-SPECIFIC AUGMENTATION
============================

python augment.py \
--sys_config sys_config.yaml \
--input_file /scratch2/rkwitt/Mount/images/image_05335_bbox_features.mat \
--object_file object_classes.txt \
--verbose \
--det_thr 0.8 \
--edn_config info_object_agnostic_full.yaml \
--output_file image_05335_bbox_features_EDN.mat \
--agnostic

OBJECT-AGNOSTIC AUGMENTATION
============================

python augment.py \
--sys_config sys_config.yaml \
--input_file /scratch2/rkwitt/Mount/images/image_05335_bbox_features.mat \
--object_file object_classes.txt \
--verbose \
--det_thr 0.8 \
--edn_config info_object_full.yaml \
--output_file image_05335_bbox_features_EDN.mat

Here, 0.8 is the detection threshold to prune detections. I would use
at least 0.7 not to get too many.

3. Load image_05335_bbox_features_EDN.mat in MATLAB, you get two variables

	1. CNN_features
	2. CNN_metadata