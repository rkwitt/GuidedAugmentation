HOWTO
=====

1) Get models:

./getmodels.sh

2) Copy models to folder on harddisk, e.g., /tmp/

/tmp/pose
/tmp/depth

3) Adjust 

sys_config_depth.yaml
sys_config_pose.yaml

4) Create a list of feature files for which to run AGA, e.g.,
   files.txt


/scratch2/rkwitt/data/SUNRGBD/one-shot/rr0/image_00298
/scratch2/rkwitt/data/SUNRGBD/one-shot/rr0/image_00511
/scratch2/rkwitt/data/SUNRGBD/one-shot/rr0/image_01131
...

For file image_00298, this assume that the following files
are available:

image_00298_bbox_features.mat	- CNN features
image_00298.jpg					- Image
image_00298_ss_boxes.mat		- Bounding boxes for detections
image_00298_ss_labels.mat		- Labels for the detections

5) Run AGA.py, for POSE

parallel python AGA.py \
	--sys_config sys_config_pose.yaml \
	--edn_config AGA_full_pose.yaml \
	--input_file {.}_bbox_features.mat \
	--label_file {.}_ss_labels.mat \
	--output_file {.}_bbox_features_OAEDN_pose.mat \
	--verbose ::: `cat files.txt`

and for DEPTH

parallel python AGA.py \
	--sys_config sys_config_depth.yaml \
	--edn_config AGA_full_depth.yaml \
	--input_file {.}_bbox_features.mat \
	--label_file {.}_ss_labels.mat \
	--output_file {.}_bbox_features_OAEDN_depth.mat \
	--verbose ::: `cat files.txt`





